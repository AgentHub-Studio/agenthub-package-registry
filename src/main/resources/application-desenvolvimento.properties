# Profile: desenvolvimento
# All *.internal hostnames resolve normally inside the Docker network.
# Only override what is genuinely different in the dev environment.

# JWT issuer via Cloudflare (HTTPS) -- tokens are issued with public URL
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://keycloak.cezar.dev/realms/{tenantId}
